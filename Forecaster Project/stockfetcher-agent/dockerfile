FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

COPY pyproject.toml README.md /app/
COPY src /app/src

RUN pip install --upgrade pip && pip install .

# outputs in container (mount this in Azure/local)
RUN mkdir -p /app/outputs
ENV OUTPUT_DIR=/app/outputs

EXPOSE 8000
CMD ["uvicorn", "stock_agent.api:app", "--host", "0.0.0.0", "--port", "8000"]



####Build and run in bash

##docker build -t stock-agent .
##mkdir -p outputs
##docker run --rm -p 8000:8000 -e OPENAI_API_KEY="$OPENAI_API_KEY" -v "$PWD/outputs:/app/outputs" stock-agent